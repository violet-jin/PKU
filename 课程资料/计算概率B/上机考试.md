http://cs101.openjudge.cn/practice/30216/
```
n=int(input())
dp=[[1]*(2**n)for _ in range(int(2**n))]
size=2**n
def dfs(x,y,size):
    if size==1:
        return
    half=size//2
    for i in range(x,x+half):
        for j in range(y,y+half):
            dp[i][j]=0
    dfs(x+half,y+half,half)
    dfs(x,y+half,half)
    dfs(x+half,y,half)
dfs(0,0,size)
for row in dp:
    print(*row)
```
（没做出来）
